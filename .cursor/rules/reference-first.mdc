---
description: 参考项目优先：开发新功能前先查参考项目是否已有现成实现，有则优先借鉴移植
alwaysApply: true
---

# 参考项目优先（Reference First）

本仓是 EDULITE_A3 结构/URDF + reBot 工具链 + trotbot RK3588/SocketCAN/MIT/PS4 平台的 ROS 2 Humble 工作区。
**开发任何新功能前，必须先确认参考项目是否已有现成实现；有则优先借鉴/移植/适配，不要重复造轮子。**

## 参考项目

| 角色 | 仓库 / 路径 | 说明 |
|------|------|------|
| **长远功能参考项目** | https://github.com/Seeed-Projects/reBot-DevArm | reBot 生态入口：Python SDK、ROS2 + MoveIt2、Pinocchio、LeRobot、Isaac Sim、视觉抓取、语音控制、Web UI 等 |
| **机械臂本体参考项目** | https://github.com/RobStride/EDULITE_A3 | 结构 / URDF / MoveIt 起点：`a3_description`、`a3_moveit_config` 来源 |
| ROS2 控制壳（本地克隆） | `../a3_arm_vendor/reBotArmController_ROS2` | MoveIt、FJT Action、service/action 接口 |
| Python/Pinocchio（本地克隆） | `../a3_arm_vendor/reBotArm_control_py` | FK/IK、轨迹、重力补偿 |
| 生产执行层 | trotbot 派生 → `a3_can_bridge`、`a3_teleop_ps4` | RK3588 / SocketCAN / MIT / PS4 |
| Wiki | https://wiki.seeedstudio.com/robotics_page/ | 官方联调说明 |

## 借鉴流程（开发前必做）

1. 新功能需求确认后（`requirements-first.mdc`），先在参考项目中检索对应实现
2. **有现成实现** → 优先借鉴，说明来源（仓库 + 文件/提交），保留上游结构再适配，不重写
3. **找不到或无法复用** → 在需求文档和 `docs/shared/CONTROL_ROADMAP.md` 标注「对标缺口」，再自行实现
4. 适配注意：关节名 `L1_joint`..`L7_joint`（L7 为夹爪）；URDF/mesh 指向 `a3_description`；
   限位以本仓 URDF + `control_gains.yaml` 为准；reBot MotorBridge 是 PC 台架**勿与** `a3_can_bridge` 同占 `can0`；
   ROS 发行版 Humble（参考项目多为 Jazzy，需降级适配）

## 边界

- 借鉴遵守上游许可（reBot-DevArm：硬件 CERN-OHL-W-2.0、软件 Apache-2.0）
- 控制能力对标与缺口统一维护在 `docs/shared/CONTROL_ROADMAP.md`（L0–L8 / Wave A·B）
