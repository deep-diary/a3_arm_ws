# Lessons Learned（踩坑与换机备忘）

> **用途：** 记录开发中踩过的坑、环境差异、耗时操作的替代方案，方便换机器 / 新人少走弯路。  
> **不是需求文档：** 正式需求仍写在 `docs/<产品线>/REQUIREMENTS.md`。

## 怎么写

每条一篇独立文件，文件名建议：

```
LL-NNN-short-slug.md
```

模板见 [TEMPLATE.md](TEMPLATE.md)。智能体在**确认修复后**按仓库规则 `.cursor/rules/lessons-learned.mdc` 自动补条目并更新本表。

写入时：现象写可检索的报错原文关键词；根因写「为什么」不是「改了哪一行」；正确做法必须是换机可执行的命令。

## 索引

| ID | 标题 | 产品线 | 日期 |
|----|------|--------|------|
| [LL-001](LL-001-microros-host-setup.md) | micro-ROS Agent / host 构建慢、apt 不可用、XRCE 消息缓冲 | CloudEdge | 2026-08-25 |
| [LL-002](LL-002-edge-sim-rviz-moveit-env.md) | Edge 仿真 RViz / MoveIt demo：HDMI、colcon overlay、缺包 | Edge | 2026-08-26 |
| [LL-003](LL-003-arm-trotbot-port-leftovers.md) | trotbot→arm 移植遗留：限位表越界把目标 clamp 成 0、脚本 CRLF 报 pipefail | Edge | 2026-09-01 |
| [LL-004](LL-004-lubancat-can0-can1-physical.md) | LubanCat-4-V1 CAN 物理口：软件 can0 无收发器、can1（芯片 CAN2）才能直连电机 | Edge | 2026-09-01 |
| [LL-005](LL-005-humble-timer-qos-python-node.md) | Humble Python 节点：create_timer 无 oneshot；/joint_states 需 best-effort 订阅 | Edge | 2026-09-02 |
| [LL-006](LL-006-ros2-param-type-mismatch.md) | ROS 2 参数：declare_parameter 默认值类型必须与 params-file 一致 | Edge | 2026-09-03 |
| [LL-006](LL-006-test-proc-cleanup-domain-isolation.md) | 测试脚本后台 ros2 launch 需 setsid 整组清理；mock 用独立 ROS_DOMAIN_ID 隔离；pkill -f 防自匹配 | Edge | 2026-09-02 |
| [LL-007](LL-007-servo-zero-pose-ik-singularity.md) | MoveIt Servo 在全零位（L2/L3 限位边界+奇异）IK 失败 -31，需先预定位到 home 非奇异位再 jog | Edge | 2026-09-02 |
| [LL-008](LL-008-paho-mqtt-v2-on-disconnect-signature.md) | paho-mqtt 2.x VERSION2 回调 5 参数：on_disconnect 只写 4 形参 → 断线 TypeError、无法重连 | Edge | 2026-09-06 |
| [LL-009](LL-009-arm-control-mode-no-release.md) | /a3/control_mode 只发 TRAJ_RUNNING 不回收 + VOLATILE 启动发布丢消息：真机夹爪力控互锁永久锁存 | Edge | 2026-09-06 |
| [LL-010](LL-010-gripper-force-timeout-ignored.md) | GripperCommand.timeout_s 被忽略：软物体力环未收敛就 ERR_GRASP_TIMEOUT | Edge | 2026-09-06 |
| [LL-011](LL-011-nan-poisons-json-telemetry.md) | NaN 毒化 JSON：allow_nan=True 产出非法 JSON，浏览器 JSON.parse 整包丢弃（Python json.loads 容忍 NaN 掩盖问题） | Edge | 2026-09-06 |
| [LL-012](LL-012-rclcpp-deferred-service-response.md) | rclcpp Humble 延迟服务响应必须用两参数 (header,req) DeferResponse 回调；三参数 (header,req,resp) 返回即自动回包 | Edge | 2026-09-06 |
| [LL-013](LL-013-gripper-hardstop-torque-false-contact.md) | 0 位硬止位静置力矩（≈0.16 Nm）≥ 接触阈值致起步误判接触 + web 力控不传 timeout 落 5s 默认 → 真机力控卡 42% 开合 | Edge | 2026-09-06 |
| [LL-014](LL-014-gripper-stuck-trajectory-stream-overheat.md) | 轨迹插值卡死持续流送旧目标 + 单帧卸力被覆盖：电机顶泡棉 1.55 Nm 过热 125°C；卸力=停流送器+cansend 零力矩帧作最后一帧 | Edge | 2026-09-06 |
| [LL-015](LL-015-f32-short-traj-starvation.md) | F32 回归：单点轨迹「只出 2 帧且无效」=命名轨迹索引回退误驱 L1 + 启动平滑吞 0.05s 短轨迹 + traj/refresh 共享限速戳饿死 traj 帧 | Edge | 2026-09-06 |
| [LL-016](LL-016-rclpy-sigint-waitset-race.md) | rclpy Humble SIGINT 与 WaitSet 竞态：节点退出时 RCLError 穿透 spin 致 exit code 1；须在 main 捕获（RCLError 只在 rclpy._rclpy_pybind11 私有模块） | Edge | 2026-09-07 |
| [LL-017](LL-017-mit-hold-end-refresh-kp80.md) | MIT hold 结束后 refresh 以默认增益 kp=80 续推旧目标角：止位处被固件钳 1 Nm 持续顶死发热（33→56°C）；stop 后残余力矩是楔入止位的回弹外载 | Edge | 2026-09-07 |
| [LL-018](LL-018-generic-arm-bringup-three-pits.md) | 通用 N 关节臂接入三坑：电机不主动上报反馈须先播种轨迹；新增 yaml 配置须清 build 目录重编（glob 对 build 副本生效）；/joint_states 是 BEST_EFFORT 订阅要同 QoS | Edge | 2026-09-11 |
| [LL-019](LL-019-multiturn-wrap-zero-sta-firmware-split.md) | 断电转动丢多圈：zero_sta(0x7029)=0 默认 0~2π 窗口致负转 +2π 环绕（-22° 读 +338°，机器狗暴动机理）；窗口仅上电时生效；save 帧数据域须 01..08 否则不保存；RS00 固件 0.0.3.4 读回恒 0 但写+保存生效（判断以断电行为为准）；环绕读数超限严禁使能（F48 已加使能门禁） | Edge | 2026-09-13 |
| [LL-020](LL-020-bridge-no-seed-stale-js.md) | 桥无指令历史时 refresh 不播种 → 总线静默 → /joint_states 冻结旧值（数值对但数据旧）；修复：零增益保活帧播种（模式未知/失能时）+ 校验脚本与使能门禁查 stamp 新鲜度（max 1 s）；任何读 js 判状态的流程先验新鲜度 | Edge | 2026-09-13 |
| [LL-021](LL-021-grasp-timeout-kills-established-grasp.md) | 力控抓取超时把已 GRASPED 的抓取判死：软泡棉滑脱振荡使状态在 GRASPED↔FORCE_CLOSING 往返，超时只问「当前是否 GRASPED」不问「是否曾抓稳」；修复 `_grasped_ever`——超时仅约束进入 GRASPED 前的时限；命令别传短于默认 15 s 的 timeout | Edge | 2026-09-13 |
| [LL-022](LL-022-service-enable-no-stream-limp-arm.md) | 服务直驱使能后桥无目标流 → 已使能电机静默无力矩（悬空臂瘫软，冻结 js 假性「无漂移」）；02 反馈帧扩展 ID bit22-23 解析模式状态 + tx_stats tx_hz + fresh 三件套诊断；播种三态化：已使能+反馈新鲜 → 锚定反馈位 runtime 增益真实保位 | Edge | 2026-09-13 |
| [LL-023](LL-023-temp-threshold-65-too-low.md) | F44 温度阈值 65°C 过低：真机 ready 位 L3 保位 1.26 Nm 几分钟即 63→65°C 触发 overtemp park（保护路径真机首触发工作正常）；官方电机自带 130°C 兜底，阈值调 warn=90/protect=95；记录位姿必须以新鲜反馈为准 | Edge | 2026-09-13 |
| [LL-024](LL-024-codec-per-motor-torque-scale.md) | 力矩/速度编解码统一 ±6 Nm/±50 rad/s：RS00（L1-L3）反馈力矩少报 2.333 倍、τ_ff 反向放大 2.333 倍；按电机型号量程编解码（RS00 ±14/±33，EL05 ±6/±50，config motor_torque_range_nm/motor_speed_range_rad_s）；F42 钳位阈值同步按型号（RS00 5 Nm） | Edge | 2026-09-13 |
| [LL-025](LL-025-zero-torque-frozen-model-shove.md) | 零力矩启动 1.5s 推飞机械臂：gravity 节点 RELIABLE 订 BEST_EFFORT 的 /joint_states 致模型冻结全零（常数前馈）+ 单点位经验增益外推 + _pin 初始化顺序致官方标定参数从未加载；启动前验证协议：τ_g 必须随姿态变化 | Edge | 2026-09-13 |
| [LL-026](LL-026-txstats-window-and-torque-latch-release.md) | F46 tx_stats 5s 窗口旋转切分轨迹尾巴（瞬时读 tx_traj_total=7 vs 实际 4098，读帧率须对照同时段桥日志）+ F42 latch 释放余量 0.02 rad 在慢速跟踪（滞后 ~0.006 rad）中不可达 → 残留 latch 钉死新轨迹（回程/park 卡死超时 FAULT）→ 新轨迹清 latch 修复 | Edge | 2026-09-13 |
| [LL-027](LL-027-rviz-panfrost-freeze-software-render.md) | RK3588 上 RViz 硬件 GL 渲染瞬间整机硬卡死（panfrost GPU hang 疑似，日志全丢只能断电）：卡死特征 = journald 停更 + 子进程日志 0 字节/尾部 NUL；RViz 一律 `LIBGL_ALWAYS_SOFTWARE=1` 软件渲染规避（launch 用 `prefix="env ..."`，本机 Node 无 additional_env） | Edge | 2026-09-13 |
| [LL-028](LL-028-robot-state-publisher-frame-prefix.md) | RViz 双模型：rsp 前缀参数名是 `frame_prefix`（`tf_prefix` 被静默忽略 → 两个 rsp 同帧名互踩，实际模型在两个状态间来回跳）；`frame_prefix` 直接拼接须带尾斜杠 `"target/"`（RViz TF Prefix 查找自动加 /）；rsp 不发根帧需恒等静态变换接树；TF 链验证用 Python listener + 4s 预热 | Edge | 2026-09-13 |
| [LL-029](LL-029-l7-gripper-zero-drift-f48-hard-fail.md) | 夹爪全开止点读数漂负（设零间隙产物，-0.023 越下界）触发 F48 硬校验 FAIL + 使能门禁拒绝；正确处置是单独 set_zero（motor_id=7）让止点归 0（止点处误差=0、无残余压指力矩），而非放宽限位（会把标定漂移固化进 URDF/F48/门禁）；先区分「连续小偏移=标定漂移」与「±2π 跳变=断电环绕」；编排层直连服务流须桥 `use_power_sequence:=false`（默认 true 时 gate 关闭静默阻断轨迹、Running 时 F32 互锁拒 enable/reset/set_zero） | Edge | 2026-09-13 |
| [LL-030](LL-030-sim-real-js-qos-divergence.md) | 真机 /joint_states 是 BEST_EFFORT（motor_protocol），仿真节点是 RELIABLE：默认 RELIABLE 订阅在真机静默零投递——FJT 动作收敛判定恒失败（臂已到位仍 timeout code=-5）、IK 种子恒零、js 镜像断流；三处订阅显式 BEST_EFFORT 修复；「仿真过真机静默挂」先 `ros2 topic info -v` 对比两端 QoS | Edge | 2026-09-13 |
| [LL-031](LL-031-ik-limit-index-jid-vs-idx-qs.md) | Pinocchio lowerPositionLimit/upperPositionLimit 是 nq 维数组按 **idx_qs** 索引：按 joint id 索引使每关节被钳到「下一关节」的限位（L2 砸到 -0.05 越物理界、L1/L6 强制 ≥0.05）→ 良态位姿 IK 200 步永不收敛；收敛校验同错位会放行真越界解；同场确认：雅可比帧 LOCAL 正确（WORLD 帧 lstsq 发散）、Quaternion 构造序 (w,x,y,z)、home 位 L3 顶限位 margin 死锁（home 区域用 move_to 不用 IK）、IK action「发布即 succeed」验证要等收敛 | Edge | 2026-09-13 |
| [LL-032](LL-032-ds4-v2-usb-hid-sony-probe-fail.md) | PS4 手柄 USB 有线（054c:09cc）：vendor 内核 hid-sony 探测失败 `failed to claim input` 无 js 节点（蓝牙正常）；设备 HID 分组 g0000 使 hid-generic/bind/new_id 全不接管 → 自编迷你模块 ds4_generic（hid_parse+hid_hw_start，~20 行，`~/ds4_drv/` 用板载 headers 编译）兜底认领出 js0；有线布局与 hid-sony 不同须 joy_dump 重校准 yaml | Edge | 2026-09-13 |
| [LL-033](LL-033-gravity-inertia-map-off-by-one.md) | 重力标定杆映射错位一位：body i = 关节 i 子杆，官方 inertias[2..6] = 各关节**下方**杆，旧映射上移一杆（L2→l1_link 只影响 τ₁）→ 标定值落错杆 + 拟合 L2 参数梯度恒零被平坦方向补偿掩盖；验收必须判 τ 预测精度（参数找回不是有效判据） | Edge | 2026-09-13 |
| [LL-037](LL-037-js-effort-motor-domain-and-fit-bounds.md) | `/joint_states` 三个字段域不一致：position/velocity 已换算到 URDF 域、**effort 是电机域原值**（未乘 `joint_signs`）——重力标定不换算 → sign=−1 的 L1/L3/L5 符号全反，RMSE 1.66 且坏参数直接落盘；换算用 `joint_signs`（`control_gains.yaml`）。次坑：官方 `FIT_BOUNDS` 把本臂 URDF 现值排除在外（L4 com_y/L5 com_z/L6 com_z 最优解顶边界）→ 初值取 URDF 现值、边界放宽，并打印顶边界告警 | Edge | 2026-09-14 |
| [LL-038](LL-038-static-gravity-calibration-hardware-floor.md) | 静态重力标定 0.1592 Nm 是本臂**硬件地板**：20 参数全一阶矩 0.158 ≈ 12 参数 0.159，再放开腕部关节原点几何也不降（几何/质量都被排除）；同姿态跨会话/跨温度残差只差 0.03 Nm（可复现非噪声）→ 传动/打印件柔性/线束等非刚性效应，「扣掉方向项」也解释不全。**诊断坑**：按相邻 Δq 符号分上下行统计"摩擦"会被区域混淆（本网格每关节只单向扫，下行样本=折返点），必须先验位置分布可比性。验收位姿取 free 位（ready），折叠 home 搭支撑 τ≈0 判不了（LL-035） | Edge | 2026-09-14 |
| [LL-039](LL-039-teach-exit-reanchor-false-trip-enable-snap.md) | **真机事故：甩断 L6**。三层叠加：①看门狗在示教退出（执行层 F38 正确重锚目标）后仍拿**上一条轨迹末点**当保持参照 → 1 s 持续窗满即假触发 HOLD_DRIFT（运动窗 `_traj` 同样未关 → 仿真里先触发 FOLLOW_STUCK）；②`motor_stop` 写的 NaN 被 refresh 播种分支 5 ms 内覆盖（卸力帧不改 mode=2，播种判定「已知使能+反馈新鲜」→ 锚回拖动位姿），日志里 `cmd_angle=nan` **0 次**；③失能期目标仍 1.98 rad 而实际 0.0286（人工搬回 home），使能无任何「目标-实际」校验 → kp=80 满增益 → L2 3.1 s 冲 1.94 rad，F42 是防撞不防甩 → L6 打印件断裂。修复 F51：stop/reset 保持抑制 latch、使能重锚到反馈位（反馈陈旧则拒绝使能）、kp 软起步 0.8 s、失能期陈旧目标 WARN；看门狗「意图边界重基准」+status(PENDING/TRIGGERED) 口径；编排层消费 UNEXPECTED_DISABLE（看门狗持续窗须短于本地兜底，否则真故障报不出来）。判据：**假阳性修复必须同时验真阳性**、**回归要证明旧代码会失败**、**测试与真机栈 DDS 隔离**、**`ros2 run` 须按进程组杀** | Edge | 2026-09-14 |
| [LL-034](LL-034-monitor-service-deadlock-and-clock-epoch.md) | 看门狗节点两隐蔽坑：①timer 回调内 `spin_until_future_complete` 死锁——单线程 executor 下响应回调永远排不进队列，服务端已执行客户端全超时（看门狗形同虚设）→ 服务客户端挂独立 ReentrantCallbackGroup + MultiThreadedExecutor + 纯轮询等 future；②`time.monotonic()` 减消息墙钟 stamp 纪元混减恒为负 → STALE_JS 永不触发 → 过期判据记接收时刻 monotonic | Edge | 2026-09-13 |
| [LL-040](LL-040-enable-resurrects-unnamed-joint-input.md) | 使能后「未点名关节」的历史输入会复活：F51 重锚只重锚命名目标，漏了 Named 轨迹的**回退表** `latest_input_champ_rad_`/`has_latest_input_` → 使能前满关节轨迹的旧值会被部分点名轨迹（如只点名 L7；5J 档下不点名任何档位关节）回放驱动。判据：**任何「目标/缓存失效」都要问「还有哪些同源副本没清」**；回归 T5 + 验牙齿（旧代码 L2→0.6） | Edge | 2026-09-14 |
| [LL-042](LL-042-bus-silence-motors-keep-last-command.md) | **执行层进程死亡 = 总线静默**：真机实测 85 s 后 5/5 仍 `mode=2`、位置逐关节不变 → RS00/EL05 **无通信超时保护，一直保持最后一条 MIT 命令**（不会掉，但 F42/F44/F50 全没了）；且看门狗动作走 `/a3/motor/{stop,reset}` **服务、由刚死的 motor_protocol_node 提供** → 动作发不出去，`STALE_JS` 又只在 READY/TRAJ/SAFE_PARK/SERVO/TEACH/AI 评估（本例停在 IDLE 连检测都没有）。测法坑：`probe` 发零增益帧会**破坏被测状态**，`candump` 0 帧=电机不主动广播 | Edge | 2026-09-15 |
| [LL-041](LL-041-rest-pose-outside-urdf-limit-f48-refuses-enable.md) | 关节静置在 URDF 限位外 → **编排层 F48 拒绝使能**（`position check failed: L4_joint=-1.0981 limit=-1.0472..1.5708`），执行层 `/a3/motor/enable` 却能过：事故后肘部静置平衡位 -1.099 rad 比官方额定下界低 0.052 rad（不是 ±2π 环绕，**不许放宽限位**）；处置=失能态手动抬回限位内，或用验收脚本 `--nudge-delta`（执行层使能 + 2.5 s 小幅插值）后在使能态调 `/a3/arm/enable` | Edge | 2026-09-14 |
| [LL-043](LL-043-mit-zero-frame-in-startup.md) | 机械臂启动别再发机器狗残留的 MIT「掰零」帧：`send_mit_zero_in_startup: true` 会向全臂发 pos=0,kp=80（L4@+1.17rad ≈94 Nm 甩臂）；改 false，start=使能+开 gate，由 F51 零增益保活帧锚定当前位接管——**上电/启动路径不得把固定目标角满增益塞整臂，目标必须从当前反馈推导** | Edge | 2026-09-15 |
| [LL-044](LL-044-disable-park-3s-too-fast.md) | disable park 回 home 过急（3 s，行程可 >1 rad）触发看门狗 `FOLLOW_STUCK→stop→3s→reset` 阶梯在 SAFE_PARK 中把臂切断、误报「safe park aborted: 电机带外失能」；看门狗不识别 park 是自发的受控运动 → `disable_home_duration_s` 3.0→**5.0**，回 home 慢无妨 | Edge | 2026-09-15 |
| [LL-045](LL-045-disable-in-zero-torque-dead-path.md) | 零力矩手感下 `/a3/arm/disable` 走死路径：F40 safe park 的 home 轨迹被执行层 OnTrajectory **静默丢弃**（`zero_torque_active_`）→ 臂悬浮干等 → 看门狗 FOLLOW_STUCK 阶梯误切 + 误报带外失能；且 reset/enable 都**不退出 zero_torque**（enable 伪成功，kp 仍 0；reset 撤重力补偿垂落）。F53 修 5 处编排层 mode×state 校验；口诀：**编排层任何依赖臂动的指令必须校验 state×mode，mode 吞轨迹时所有安全回退失效** | Edge | 2026-09-16 |
| [LL-046](LL-046-playback-ramp-2p5s-too-fast.md) | 回放前 ramp 2.5s 过急：末位→首位大差（L4 深垂 1.18 rad）压进 2.5s 形成 ~0.47 rad/s 下砸，「回放开局快」；`playback_ramp_duration_s` 2.5→**5.0** 拉平（真机 jog2 实测 0.047 rad/s，肉眼平滑） | Edge | 2026-09-16 |
| [LL-047](LL-047-playback-smoothing-accel-spike.md) | 回放「稍微抖动」不是伺服（最优滞后 0.00s、帧抖动 2.9 mrad）而是**录制的加速度尖峰**（手拖 50Hz，L2 113 rad/s²）被忠实复现；**再示教无用，得播放时软件低通**——`playback_smooth_samples` 中心滑动平均只动 positions 保留时间戳，L2 -92%（119→9.5 rad/s²）、路径扰动 ≤20 mrad | Edge | 2026-09-16 |
| [LL-050](LL-050-ros2-cli-discovery-overhead-timing.md) | `ros2 service call` CLI 首次/冷进程有 **~3 s DDS discovery 开销**：bash 里 start_teach→sleep 0.04→stop_teach 实测变成 ~151 样本（≈3 s）误当正常录制、误触发阈值判假 FAIL；**时序/阈值/次数语义的验收用纯 rclpy 单进程 `call_async` + `time.monotonic()` 打点**，实测 0.05 s 记录 = 3 samples 正确触发 skip | Edge | 2026-09-16 |
| [LL-051](LL-051-yaml-description-colon-space.md) | YAML `description:` 值里「冒号+空格」（`门禁: 目标`、`F40: 离`）被解析成嵌套映射 → `yaml.safe_load` ScannerError；`colcon build` 不报错、运行时才炸。**值内出现 `: `/`{`/`[` 一律加引号**；排查 `grep -n 'description:.*: '` | Edge | 2026-09-17 |
| [LL-052](LL-052-bluetooth-ds4-touchpad-not-in-js0.md) | 蓝牙 DS4 触摸板按键在独立 input 设备（`input41` "Wireless Controller Touchpad"），**不进 js0** → `/joy` 按钮数组里索引 13 永为 0，F55 `touchpad=arm_init` 真机失效；**init 改绑 `ps`（索引 10，实测可按键）**；逐键确认时查 `/proc/bus/input/devices` 确认按键属于哪个设备体 | Edge | 2026-09-17 |
| [LL-053](LL-053-playback-ripple-stop-gravity-hold.md) | ①**MIT 帧 V 域恒 0** → kd 按 `kd·(0−v_act)` 拖刹车 → 20ms 速度纹波 rms0.146（V 填目标速度=vel FF，差分平滑后目标 champ_smoothed 保逐 tick 一致）；②回放轴=手拖轴，位置平滑治不了起-停 → 时间轴匀速重排（`_time_warp_points`，vmax 0.6 rad/s / dt_min 0.02）+ 保存时 w=5 轻平滑；③**stop=裸卸力危险位掉臂**（悬臂/水平轴无约束力矩直接垂落）→ stop/keepalive 反馈新鲜时改 `kp=stop_hold_kp+kd+重力前馈` 重力支撑保持；自动 reset 过 `max\|τ_grav\|≤5 Nm` 新鲜门禁，拒则 RESET_DENIED 保持等人工（F56/F57/F58） | Edge | 2026-09-17 |
| [LL-054](LL-054-fastdds-ghost-participant-kill9.md) | 反复 `kill -9` rclpy 注入器 → FastDDS 遗留幽灵 participant（无 DISPOSE）污染本机 discovery：之后**任何新起的 participant 的 reliable publish 卡死**（含全新 ROS_DOMAIN_ID）；真机栈既有连接不受影响。规避：长循环注入进程一律优雅 `rclpy.shutdown()`；污染后重启整栈 + 清 `/dev/shm` fastrtps 段；端到端验收选一次性干净域；`timeout` 杀卡死 python 带 `-k` | Edge | 2026-09-17 |
| [LL-055](LL-055-rsp-missing-joint-no-tf.md) | `robot_state_publisher` **只对 joint_states 消息里出现的关节名发布 TF**：名字缺失的关节不按 URDF 默认位渲染，整个下游分支（含固定关节子链）无变换 → RViz no transform。注入/核对模型时关节名必须放齐；缺失排查先看上游关节名是否在数据源消息里。停止发布后 rsp 缓存最后一条继续重发，缺失只在「从未出现过」时才发生 | Edge | 2026-09-17 |
| [LL-056](LL-056-claude-code-settings-env-overrides-bashrc.md) | Claude Code 的供应商配置**以 `~/.claude/settings.json` 的 `env` 块为准**（settingsEnv 覆盖继承的 shell env），`.bashrc` 里 export 的 `ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL` 被**静默覆盖**——`env` 里看到的不等于实际请求的端点。换供应商只改 settings.json 一处（主模型 + `DEFAULT_{OPUS,SONNET,HAIKU}_MODEL` + `CLAUDE_CODE_SUBAGENT_MODEL` 同改）；验证用 `claude --debug --debug-file <log> -p '…'` 后 grep 日志里的 base_url，别只看 `env`；Ark 的 `/api/plan/v3` 只对 OpenAI 协议工具（codex）有意义 | Edge | 2026-09-18 |
| [LL-057](LL-057-warp-reinflation-marginal-l2-torque.md) | F57 时间重排真机两坑：①零位移停顿段每点吃 0.02s 地板→停顿重新充气（16.6s 示教→30.9s 回放），改弧长均匀重采样、零弧长段不占时间；②逐点加速度恰解在真实数据上被「最大关节换帅」离散伪影（top-20 \|Δv\| 全部精确 =v_eff=0.274）+编码器噪声带飞，前后向往返膨胀到 38.8s——限幅改到步长 5 粗网格（0.1s 窗，噪声底 ~5→0.4 rad/s²），只约束持续需求（FOLLOW_STUCK 是持续饱和不是单步暂态），实测 21.6s→21.8s、粗网格 accel 0.98 | Edge | 2026-09-18 |
| [LL-058](LL-058-ghost-all-white-no-injection-source.md) | 双模型 RViz ghost 全白/no transform：target rsp **零条 joint_states 时整树（含固定关节）不发 TF**，恒等静态 TF 只接根帧；主 launch 挂了 target rsp 却没注入源。修：`urdf_dir_check_pub` 加 `publish_traj:=false` ghost-only 模式（不碰执行层轨迹）+ 外来注入内容比对让位 3 s 看门狗；真机/sim 两 launch 默认挂三件套、`use_target_ghost:=false` 可关。验证坑：/tf 是 volatile、/tf_static 是 transient_local（QoS 套错零数据）；rclpy 暖机必须 spin；rsp 存活 ≠ 动态 TF 在发（joint_states 源死后树断两棵） | Edge | 2026-09-18 |
| [LL-059](LL-059-sim-reliable-masks-teleop-best-effort.md) | QoS 同族坑第四次：teleop `actions.py`/legacy `ps4_arm_teleop.py` 默认 RELIABLE 订 `/joint_states`，真机 BEST_EFFORT 静默零投递（`_have_js` 恒 False、命名位姿键全失效，日志 skip named pose），仿真 RELIABLE 发布再次掩盖；两处显式 BEST_EFFORT 修复。规则：**凡新增 JointState 订阅必须 BEST_EFFORT**，grep 逐个核对 | Edge | 2026-09-20 |
| [LL-060](LL-060-pygame-sdl-6axis-vs-joydev-8axis.md) | Linux DS4 两栈不可混用：pygame/SDL 6 轴+hat、摇杆右/下正、扳机静息 **−1**；joydev/joy_node 8 轴（6/7=dpad）、raw 左/上正、扳机静息 **+1**。旧 DS4_LINUX 照抄 joydev 索引+auto 误判 ds4_sdl → 键位全错；修：独立 ds4_linux 表（按 idle axis2<−0.5 检测）、hat 出十字键，ROS 侧只由 ds4_tcp_joy_node 一处做极性转换。另：ROS source 的 PYTHONPATH 污染 venv，pytest 加载 ROS 插件报缺 `lark`，须 `env -u PYTHONPATH -u AMENT_PREFIX_PATH` 跑 | Edge | 2026-09-20 |
| [LL-061](LL-061-evdev-raw-events-arbiter-pressed-wrong-direction.md) | 极性争议第一步直接读 evdev 原始事件（/dev/input/eventN，20 行 Python，无需 jstest/evtest）：实测十字键上=−1/下=+1，内核与 joy_node 一直正确，窗口里的 +1 是按错方向；同设备多 joy_node 不构成污染，异源（TCP 桥）混入才污染 | Edge | 2026-09-20 |
| [LL-062](LL-062-include-launch-config-scope-leak.md) | `IncludeLaunchDescription` 不隔离 `LaunchConfiguration`：子 launch 传 `use_rviz:=false` 等于全局重设同名变量，把外层自有 RViz 的 IfCondition 静默顶假（无 warning，进程根本不 spawn）；需要重名不同值就用 scoped `GroupAction([include])` 包住，include 前先 diff 双方参数名 | Edge | 2026-09-20 |
| [LL-063](LL-063-pick-ik-orientation-threshold-kills-servo-yaw.md) | servo 偏航满杆静默（出帧但零速度，status=0）：`kinematics.yaml` 的 pick_ik `orientation_threshold:0.01` ≥ 满杆常速每 tick 增量（1.0×0.35×0.02=0.007 rad），local 求解器首步判定 seed 已达标直接返回；0.50 rad/s（0.010/tick）才动。阈值必须 < 最小可控角速度×publish_period，恢复默认 0.001；排查"servo 出帧不动"先旁路 TwistStamped 幅度二分 | Edge | 2026-09-20 |
| [LL-064](LL-064-periodic-status-hides-short-states.md) | init 后 DS4 无白闪：`_set_state` 只改内部态、ArmStatus 仅周期发布，sim 里 INIT 仅存活几 ms（set_zero 即刻 7/7），没有任何一帧携带 INIT；下游 15Hz 采样再漏一层。状态边沿必须即时发布（`_build_status_msg` 共用），必应边沿在订阅回调里消息粒度检测并锁存；真机 INIT≈1s 的 CAN 往返掩盖了缺陷 | Edge | 2026-09-21 |
| [LL-065](LL-065-ogre-randr-zero-rates-empty-modelist.md) | RViz 启动即 SIGSEGV：X RANDR 报告尺寸但零刷新率 → Ogre 视频模式表为空 → `possibleValues[0]` 空指针；LD_PRELOAD shim 隐藏 RANDR 走单模式回退（launch 已自动接入）；附 root 进程 environ 盲区、DPMS 黑屏截图、跨域误判 | Edge | 2026-09-21 |
| [LL-066](LL-066-pgrep-launch-string-matches-agent-bash-wrapper.md) | grep "ros2 launch …" 找进程组会自匹配 Bash 工具的 `/bin/bash -c … eval …` 包装层（命令串在 argv 里），先杀错组而真 launch 组还活着；锚定 `/bin/ros2 launch` 真实可执行体并用 awk 排除 bash，组信号前先列全组成员核对 | Edge | 2026-09-21 |
| [LL-067](LL-067-bt-ds4-hidraw-udev-access.md) | 蓝牙 DS4 hidraw 默认 root 0600：`DRIVER=="sony"` 在 hidraw 设备不匹配（用 KERNELS 父级 modalias）、`TAG+="uaccess"` 对非 seat 会话不授 ACL（用 GROUP="dialout"+MODE 0660）；trigger 不生效时加 `--action=add` | Edge | 2026-09-21 |
| [LL-068](LL-068-sdl2-hidapi-ds4-6axis-layout.md) | ros-humble-joy 是 SDL2 实现，蓝牙 DS4 默认走 HIDAPI 报「PS4 Controller」6 轴 16 键（dpad 变按钮）索引全错；`SDL_JOYSTICK_HIDAPI=0` 强制 evdev 回 8 轴 13 键（旧变量名 SDL_HIDAPI_JOYSTICK 无效） | Edge | 2026-09-21 |
| [LL-069](LL-069-joy-reconnect-hidapi-multi-publisher-dual-stack.md) | L1+摇杆静默失效复发路径：蓝牙重连后常驻 joy_node 热插拔重开回 HIDAPI 6 轴（additional_env 不自愈）+ 多游离 joy_node 混发不同形状帧 + 真机/仿真同域双栈同名互串；帧形状 3 命令排查法；关节在动屏幕不动=盯错旧 RViz 窗口 | Edge | 2026-09-21 |
| [LL-071](LL-071-moveit-retime-node-two-phase-init.md) | Humble moveit_core trajectory_processing（Ruckig/TOTG）无 Python 绑定 → C++ 服务节点；参数预声明（has_parameter 守卫）；shared_from_this bad_weak_ptr（init 两阶段）；`vector<double>(0.0)` 是空 vector 致越界段错误；Ruckig 单步 update jerk 绑定致全局时长 4s→29.6s（relax_jerk_for_seed + 1.5 margin 修复） | Edge | 2026-09-22 |
| [LL-072](LL-072-ros2-control-mock-jsb-order-jtc-single-point.md) | ros2_control 标准栈仿真三连坑：JSB 早于 JTC 激活只 claim 位置接口致速度恒 0（先 JTC 后 JSB）；mock 需 calculate_dynamics=true；burst-pair 时间压缩陈旧样本+固定索引差分造 60 rad/s² 假尖峰（按真实时间 ±0.05/±0.025s 选窗）；JTC 单点轨迹是匀速线性插值有真速度阶跃（验收发 31 点五次 S 曲线）；新进程初始位取近期样本中位数 | Edge | 2026-09-22 |
| [LL-073](LL-073-diagnostic-updater-name-prefix-byte-level.md) | diagnostic_updater 三连坑：add() 任务名自动加节点名前缀（只给裸名，别手填 `节点: 组件`）；DiagnosticStatus.level 是 byte 字段 rclpy 反序列化为单元素 bytes（比较前取 x[0]）；subprocess 验收 harness 自身进程也要在 rclpy.init 前设 ROS_DOMAIN_ID，否则喂数与被测节点不在同一域 | Edge | 2026-09-22 |
| [LL-074](LL-074-ros2-control-system-interface-vcan-xacro-humble-pitfalls.md) | F72 真机 SystemInterface 插件 vcan 验收：Humble 无 xacro:elif（嵌套 if/unless）；launch Command 要 `"xacro "` 前缀否则 exec xacro 报 Errno 8；裸 read/write 需显式 `#include <unistd.h>`；install 的 launch 是 build/ 副本符号链接（改完必重编 a3_bringup）；CAN 反馈帧 motor_id 在 bits 8–15、低字节是主机 0xFD；16 位量化反馈加速度差分窗取 ±0.05 s | Edge | 2026-09-22 |
| [LL-075](LL-075-gravity-comp-controller-fullname-stale-ament-pitfalls.md) | F73 重力补偿控制器：Humble `get_name()` 返回全名 `joint/iface`（按裸名匹配 → q 恒 0）；setsid 继承安装前旧 shell 的 AMENT_PREFIX_PATH 致 pluginlib 只列 mock（source 与 setsid 同一 bash -c，查 /proc environ）；SyncParametersClient 要用独立临时节点、根 ns 防双斜杠；pinocchio `idx_q()/idx_v()` 是方法；命令句柄顺序不定须按名查找；中位数 current() 运动期滞后（实时判定用最新样本、RNEA 对帧内嵌 q、settle ≥2.5 s） | Edge | 2026-09-22 |
| [LL-076](LL-076-moveit-arm-group-leaves-l7-jtc-clamps-one-sided-limits.md) | F74 双 JTC action 后端：move_group 只规划 arm 组（L1–L6），safe-park 全绿但 L7 夹爪保持上一 goal 没归位（须显式补发 gripper FJT）；L2∈[0,3.665]/L3∈[−4.014,0] 单向关节越限目标被 JTC 静默夹紧并返回成功（落点必须逐关节复核）；测试按 /a3/arm_status.state==READY 推进，别按"关节不动了"推断 | Edge | 2026-09-22 |
| [LL-077](LL-077-goto-l7-dispatch-settle-pos-vel-double-stack.md) | F75 全产品 mock 栈：goto/move_to 走 move_group 同样漏 L7（统一 `_dispatch_l7_linear` 三入口补发、时长取 max）；只有位置 tol=0.15 无速度门会让收尾轨迹「穿越容差带」时提前失能（残差 0.055–0.110；GenericSystem 释放接口即冻结命令值，残差是真实提前失能）→ 位置≤0.02 AND 速度≤0.05 双落定+连续确认，超时一次性纠偏否则 FAULT 保持使能；重启栈先按 PID 清全部子进程，同名 controller 并存致 spawner FATAL | Edge | 2026-09-22 |
| [LL-078](LL-078-pilz-pipeline-version-coupling-circ-center-region.md) | F76 Pilz 接入四连坑：①MoveIt 升级 2.5.9→2.5.10 后第三方插件按精确次版本号链接（pick-ik 须换 20260910 重构建，自研 C++ 包重编+整组重启），陈旧 CMakeCache 残留旧 SONAME 用 `--cmake-clean-cache`；②CIRC center 辅助点经 path_constraints 传递但通用管线按 region 校验全程，region 做成 1 mm 点盒必 -2，须用覆盖整弧的 0.5 m 大盒（Pilz 只取 primitive_poses[0] 当圆心）；③Sequence 起始位姿不可达→-31，混合程序前先 PTP 归位；④管线按请求内 pipeline_id/planner_id 路由，无独立 pilz 服务 | Edge | 2026-09-22 |
